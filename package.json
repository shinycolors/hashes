{
  "name": "hashes",
  "version": "1.0.0",
  "description": "Hash Checker",
  "main": "dist/main.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "gulp",
    "start": "npm run build && node dist/index.js",
    "test:check": "npm run build && node dist/test.js",
    "lint": "tslint --project tsconfig.json --format prose"
  },
  "devDependencies": {
    "@types/buffers": "^0.1.31",
    "@types/node": "^10.0.6",
    "gulp": "^3.9.1",
    "gulp-typescript": "^4.0.2",
    "prettier": "^1.12.1",
    "tslint": "^5.10.0",
    "tslint-config-prettier": "^1.12.0",
    "tslint-plugin-prettier": "^1.3.0",
    "typescript": "^2.8.3"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "yn": "^2.0.0"
  }
}
